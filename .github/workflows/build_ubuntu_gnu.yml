name: Build libFMS test with autotools

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        distcheck-conf-flags: [--enable-openmp, --disable-openmp, --enable-mixed-mode, --disable-setting-flags FCFLAGS='-fdefault-real-8 -fdefault-double-8 -fcray-pointer -ffree-line-length-none -I/usr/include' ]
        yaml-flag: [ --with-yaml, "" ]
    container:
      image: ryanmulhall/fms-ci-minideb
      env:
        FCFLAGS: "-I/usr/include"
        VERBOSE: 1
        DISTCHECK_CONFIGURE_FLAGS: "${{ matrix.distcheck-conf-flags }} ${{ matrix.yaml-flag }}"
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Prepare GNU autoconf for build
      run: autoreconf -if
    - name: Configure the build
      run: ./configure ${DISTCHECK_CONFIGURE_FLAGS}
    - name: Build the library
      run: make -j distcheck
